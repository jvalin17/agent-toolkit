# Personal documents — never commit
HANDOFF.md
**/HANDOFF.md
*proposal*
*PROPOSAL*
*.proposal.md

# Local project memory — per-user, not shared
memory/

# Gate flags — created/cleared by hooks per session
.gates/
