{
  "mcpServers": {
    "agent-toolkit": {
      "command": "bash",
      "args": ["-c", "VENV_PY=$(bash scripts/ensure-python.sh --venv 2>/dev/null) && exec \"$VENV_PY\" -m toolkit_mcp || exec python3 -m toolkit_mcp"]
    }
  }
}
